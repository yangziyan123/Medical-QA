APP_ENV=dev
APP_NAME=medical-qa

# Docker Compose (Step 1) 默认开发数据库
DATABASE_URL=postgresql+asyncpg://medical:medical@localhost:5432/medical_qa

# JWT（开发期可用弱口令；正式环境必须改强）
JWT_SECRET=change-me
JWT_ALGORITHM=HS256
JWT_EXPIRES_MIN=10080

# Qdrant / Embeddings（Step 9）
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION=medical_chunks
EMBEDDING_DIM=384
RAG_TOP_K=5

# ---- Real LLM/Embedding (Volcengine Ark / OpenAI-compatible API) ----
# Note:
# - Model names are usually "endpoint IDs" in Volcengine Ark (e.g. ep-xxxxxx).
# - EMBEDDING_DIM must match the embedding model output dimension.
# - If you change EMBEDDING_DIM, you may need to drop/recreate the Qdrant collection.
#
# LLM_PROVIDER=volcengine
# LLM_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
# LLM_API_KEY=
# LLM_MODEL=doubao-seed-1-6-251015
# LLM_TIMEOUT_SEC=60
# LLM_MAX_TOKENS=800
# LLM_TEMPERATURE=0.2

# EMBEDDING_PROVIDER=volcengine
# EMBEDDING_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
# EMBEDDING_API_KEY=
# EMBEDDING_MODEL=ep-20251223181252-5flfx
# EMBEDDING_DIM=2560
# EMBEDDING_TIMEOUT_SEC=30
# EMBEDDING_BATCH_SIZE=32
# EMBEDDING_NORMALIZE=true

# 前端本地开发常用端口：Vite=5173，Vue CLI=3000
CORS_ALLOW_ORIGINS=["http://localhost:5173","http://localhost:3000"]
